linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
issues:
  max-issues-per-linter: 50
  max-same-issues: 10
  exclude:
    - "Error return value of .(Close|Flush|Write). is not checked"
